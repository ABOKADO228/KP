// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "association-farms-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/simple-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/simple-object-result.hxx>

namespace odb
{
  // AssociationFarmsKey
  //

  bool access::composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // farmId
    //
    t[0UL] = 0;

    // associationId
    //
    t[1UL] = 0;

    return grew;
  }

  void access::composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // farmId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.farmId_value;
    b[n].is_null = &i.farmId_null;
    n++;

    // associationId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.associationId_value;
    b[n].is_null = &i.associationId_null;
    n++;
  }

  bool access::composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // farmId
    //
    {
      ::uint64_t const& v =
        o.farmId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.farmId_value, is_null, v);
      i.farmId_null = is_null;
    }

    // associationId
    //
    {
      ::uint64_t const& v =
        o.associationId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.associationId_value, is_null, v);
      i.associationId_null = is_null;
    }

    return grew;
  }

  void access::composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // farmId
    //
    {
      ::uint64_t& v =
        o.farmId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.farmId_value,
        i.farmId_null);
    }

    // associationId
    //
    {
      ::uint64_t& v =
        o.associationId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.associationId_value,
        i.associationId_null);
    }
  }

  // AssociationFarmsEntity
  //

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  persist_statement_name[] = "persist_fasc_server_persistence_AssociationFarmsEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  find_statement_name[] = "find_fasc_server_persistence_AssociationFarmsEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  update_statement_name[] = "update_fasc_server_persistence_AssociationFarmsEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  erase_statement_name[] = "erase_fasc_server_persistence_AssociationFarmsEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  query_statement_name[] = "query_fasc_server_persistence_AssociationFarmsEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  erase_query_statement_name[] = "erase_query_fasc_server_persistence_AssociationFarmsEntity";

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::date_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  find_statement_types[] =
  {
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  update_statement_types[] =
  {
    pgsql::date_oid,
    pgsql::text_oid,
    pgsql::text_oid,
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  struct access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      pgsql::connection&,
      image_type&,
      id_image_type&,
      pgsql::binding&,
      pgsql::binding&,
      pgsql::native_binding&,
      const unsigned int*)
    {
    }
  };

  access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::id_type
  access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  id (const image_type& i)
  {
    pgsql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::init (
        id,
        i.id_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // joinDate
    //
    t[0UL] = 0;

    // status
    //
    if (t[1UL])
    {
      i.status_value.capacity (i.status_size);
      grew = true;
    }

    // notes
    //
    if (t[2UL])
    {
      i.notes_value.capacity (i.notes_size);
      grew = true;
    }

    // id
    //
    if (composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::grow (
          i.id_value, t + 3UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // joinDate
    //
    b[n].type = pgsql::bind::date;
    b[n].buffer = &i.joinDate_value;
    b[n].is_null = &i.joinDate_null;
    n++;

    // status
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.status_value.data ();
    b[n].capacity = i.status_value.capacity ();
    b[n].size = &i.status_size;
    b[n].is_null = &i.status_null;
    n++;

    // notes
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.notes_value.data ();
    b[n].capacity = i.notes_value.capacity ();
    b[n].size = &i.notes_size;
    b[n].is_null = &i.notes_null;
    n++;

    // id
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::bind (
        b + n, i.id_value, sk);
      n += 2UL;
    }
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  bind (pgsql::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    pgsql::statement_kind sk (pgsql::statement_select);
    composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // joinDate
    //
    {
      ::fasc::server::domain::Date const& v =
        o.joinDate;

      bool is_null (false);
      pgsql::value_traits<
          ::fasc::server::domain::Date,
          pgsql::id_date >::set_image (
        i.joinDate_value, is_null, v);
      i.joinDate_null = is_null;
    }

    // status
    //
    {
      ::odb::nullable< ::fasc::server::domain::AssociationFarmsStatus > const& v =
        o.status;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.status_value.capacity ());
      pgsql::value_traits<
          ::odb::nullable< ::fasc::server::domain::AssociationFarmsStatus >,
          pgsql::id_string >::set_image (
        i.status_value,
        size,
        is_null,
        v);
      i.status_null = is_null;
      i.status_size = size;
      grew = grew || (cap != i.status_value.capacity ());
    }

    // notes
    //
    {
      ::odb::nullable< ::std::basic_string< char > > const& v =
        o.notes;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.notes_value.capacity ());
      pgsql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          pgsql::id_string >::set_image (
        i.notes_value,
        size,
        is_null,
        v);
      i.notes_null = is_null;
      i.notes_size = size;
      grew = grew || (cap != i.notes_value.capacity ());
    }

    // id
    //
    if (sk == statement_insert)
    {
      // From AssociationFarms.hpp:63:76
      ::fasc::server::persistence::AssociationFarmsKey const& v =
        o.id ();

      composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::init (
        i.id_value,
        v,
        sk);
    }

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // joinDate
    //
    {
      ::fasc::server::domain::Date& v =
        o.joinDate;

      pgsql::value_traits<
          ::fasc::server::domain::Date,
          pgsql::id_date >::set_value (
        v,
        i.joinDate_value,
        i.joinDate_null);
    }

    // status
    //
    {
      ::odb::nullable< ::fasc::server::domain::AssociationFarmsStatus >& v =
        o.status;

      pgsql::value_traits<
          ::odb::nullable< ::fasc::server::domain::AssociationFarmsStatus >,
          pgsql::id_string >::set_value (
        v,
        i.status_value,
        i.status_size,
        i.status_null);
    }

    // notes
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o.notes;

      pgsql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          pgsql::id_string >::set_value (
        v,
        i.notes_value,
        i.notes_size,
        i.notes_null);
    }

    // id
    //
    {
      // From AssociationFarms.hpp:63:76
      ::fasc::server::persistence::AssociationFarmsKey v;

      composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::init (
        v,
        i.id_value,
        db);

      // From AssociationFarms.hpp:63:76
      o.id (v);
    }
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  init (id_image_type& i, const id_type& id)
  {
    pgsql::statement_kind sk (pgsql::statement_select);
    {
      composite_value_traits< ::fasc::server::persistence::AssociationFarmsKey, id_pgsql >::init (
        i.id_value,
        id,
        sk);
    }
  }

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::persist_statement[] =
  "INSERT INTO \"association_farms\" "
  "(\"join_date\", "
  "\"status\", "
  "\"notes\", "
  "\"farm_id\", "
  "\"association_id\") "
  "VALUES "
  "($1, $2, $3, $4, $5)";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::find_statement[] =
  "SELECT "
  "\"association_farms\".\"join_date\", "
  "\"association_farms\".\"status\", "
  "\"association_farms\".\"notes\", "
  "\"association_farms\".\"farm_id\", "
  "\"association_farms\".\"association_id\" "
  "FROM \"association_farms\" "
  "WHERE \"association_farms\".\"farm_id\"=$1 AND \"association_farms\".\"association_id\"=$2";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::update_statement[] =
  "UPDATE \"association_farms\" "
  "SET "
  "\"join_date\"=$1, "
  "\"status\"=$2, "
  "\"notes\"=$3 "
  "WHERE \"farm_id\"=$4 AND \"association_id\"=$5";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::erase_statement[] =
  "DELETE FROM \"association_farms\" "
  "WHERE \"farm_id\"=$1 AND \"association_id\"=$2";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::query_statement[] =
  "SELECT "
  "\"association_farms\".\"join_date\", "
  "\"association_farms\".\"status\", "
  "\"association_farms\".\"notes\", "
  "\"association_farms\".\"farm_id\", "
  "\"association_farms\".\"association_id\" "
  "FROM \"association_farms\"";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"association_farms\"";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::table_name[] =
  "\"association_farms\"";

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;
    using pgsql::update_statement;

    callback (db, obj, callback_event::pre_update);

    pgsql::transaction& tr (pgsql::transaction::current ());
    pgsql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    // From AssociationFarms.hpp:63:76
    const id_type& id (
      obj.id ());
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  erase (database& db, const id_type& id)
  {
    using namespace pgsql;

    ODB_POTENTIALLY_UNUSED (db);

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::pointer_type
  access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  find (database& db, const id_type& id)
  {
    using namespace pgsql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  reload (database& db, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    // From AssociationFarms.hpp:63:76
    const id_type& id  (
      obj.id ());

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace pgsql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::object_type >
  access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) pgsql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::fasc::server::persistence::AssociationFarmsEntity, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
