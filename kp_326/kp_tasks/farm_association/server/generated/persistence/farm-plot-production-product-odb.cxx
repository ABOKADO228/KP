// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "farm-plot-production-product-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/simple-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/simple-object-result.hxx>

namespace odb
{
  // FarmPlotProductionProductKey
  //

  bool access::composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // productId
    //
    t[0UL] = 0;

    // farmPlotId
    //
    t[1UL] = 0;

    return grew;
  }

  void access::composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (b);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);
    ODB_POTENTIALLY_UNUSED (n);

    // productId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.productId_value;
    b[n].is_null = &i.productId_null;
    n++;

    // farmPlotId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.farmPlotId_value;
    b[n].is_null = &i.farmPlotId_null;
    n++;
  }

  bool access::composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::
  init (image_type& i,
        const value_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // productId
    //
    {
      ::uint64_t const& v =
        o.productId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.productId_value, is_null, v);
      i.productId_null = is_null;
    }

    // farmPlotId
    //
    {
      ::uint64_t const& v =
        o.farmPlotId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.farmPlotId_value, is_null, v);
      i.farmPlotId_null = is_null;
    }

    return grew;
  }

  void access::composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::
  init (value_type& o,
        const image_type&  i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // productId
    //
    {
      ::uint64_t& v =
        o.productId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.productId_value,
        i.productId_null);
    }

    // farmPlotId
    //
    {
      ::uint64_t& v =
        o.farmPlotId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.farmPlotId_value,
        i.farmPlotId_null);
    }
  }

  // FarmPlotProductionProductEntity
  //

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  persist_statement_name[] = "persist_fasc_server_persistence_FarmPlotProductionProductEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  find_statement_name[] = "find_fasc_server_persistence_FarmPlotProductionProductEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  update_statement_name[] = "update_fasc_server_persistence_FarmPlotProductionProductEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  erase_statement_name[] = "erase_fasc_server_persistence_FarmPlotProductionProductEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  query_statement_name[] = "query_fasc_server_persistence_FarmPlotProductionProductEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  erase_query_statement_name[] = "erase_query_fasc_server_persistence_FarmPlotProductionProductEntity";

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::int4_oid,
    pgsql::int4_oid,
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  find_statement_types[] =
  {
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  update_statement_types[] =
  {
    pgsql::int4_oid,
    pgsql::int4_oid,
    pgsql::int8_oid,
    pgsql::int8_oid
  };

  struct access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      pgsql::connection&,
      image_type&,
      id_image_type&,
      pgsql::binding&,
      pgsql::binding&,
      pgsql::native_binding&,
      const unsigned int*)
    {
    }
  };

  access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::id_type
  access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  id (const image_type& i)
  {
    pgsql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::init (
        id,
        i.id_value,
        db);
    }

    return id;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // quantity
    //
    t[0UL] = 0;

    // productionNow
    //
    t[1UL] = 0;

    // id
    //
    if (composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::grow (
          i.id_value, t + 2UL))
      grew = true;

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // quantity
    //
    b[n].type = pgsql::bind::integer;
    b[n].buffer = &i.quantity_value;
    b[n].is_null = &i.quantity_null;
    n++;

    // productionNow
    //
    b[n].type = pgsql::bind::integer;
    b[n].buffer = &i.productionNow_value;
    b[n].is_null = &i.productionNow_null;
    n++;

    // id
    //
    if (sk != statement_update)
    {
      composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::bind (
        b + n, i.id_value, sk);
      n += 2UL;
    }
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  bind (pgsql::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    pgsql::statement_kind sk (pgsql::statement_select);
    composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::bind (
      b + n, i.id_value, sk);
  }

  bool access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // quantity
    //
    {
      int const& v =
        o.quantity;

      bool is_null (false);
      pgsql::value_traits<
          int,
          pgsql::id_integer >::set_image (
        i.quantity_value, is_null, v);
      i.quantity_null = is_null;
    }

    // productionNow
    //
    {
      int const& v =
        o.productionNow;

      bool is_null (false);
      pgsql::value_traits<
          int,
          pgsql::id_integer >::set_image (
        i.productionNow_value, is_null, v);
      i.productionNow_null = is_null;
    }

    // id
    //
    if (sk == statement_insert)
    {
      // From FarmPlotProductionProduct.hpp:60:94
      ::fasc::server::persistence::FarmPlotProductionProductKey const& v =
        o.id ();

      composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::init (
        i.id_value,
        v,
        sk);
    }

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // quantity
    //
    {
      int& v =
        o.quantity;

      pgsql::value_traits<
          int,
          pgsql::id_integer >::set_value (
        v,
        i.quantity_value,
        i.quantity_null);
    }

    // productionNow
    //
    {
      int& v =
        o.productionNow;

      pgsql::value_traits<
          int,
          pgsql::id_integer >::set_value (
        v,
        i.productionNow_value,
        i.productionNow_null);
    }

    // id
    //
    {
      // From FarmPlotProductionProduct.hpp:60:94
      ::fasc::server::persistence::FarmPlotProductionProductKey v;

      composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::init (
        v,
        i.id_value,
        db);

      // From FarmPlotProductionProduct.hpp:60:94
      o.id (v);
    }
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  init (id_image_type& i, const id_type& id)
  {
    pgsql::statement_kind sk (pgsql::statement_select);
    {
      composite_value_traits< ::fasc::server::persistence::FarmPlotProductionProductKey, id_pgsql >::init (
        i.id_value,
        id,
        sk);
    }
  }

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::persist_statement[] =
  "INSERT INTO \"farm_plot_production_product\" "
  "(\"quantity\", "
  "\"production_now\", "
  "\"product_id\", "
  "\"farm_plot_id\") "
  "VALUES "
  "($1, $2, $3, $4)";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::find_statement[] =
  "SELECT "
  "\"farm_plot_production_product\".\"quantity\", "
  "\"farm_plot_production_product\".\"production_now\", "
  "\"farm_plot_production_product\".\"product_id\", "
  "\"farm_plot_production_product\".\"farm_plot_id\" "
  "FROM \"farm_plot_production_product\" "
  "WHERE \"farm_plot_production_product\".\"product_id\"=$1 AND \"farm_plot_production_product\".\"farm_plot_id\"=$2";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::update_statement[] =
  "UPDATE \"farm_plot_production_product\" "
  "SET "
  "\"quantity\"=$1, "
  "\"production_now\"=$2 "
  "WHERE \"product_id\"=$3 AND \"farm_plot_id\"=$4";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::erase_statement[] =
  "DELETE FROM \"farm_plot_production_product\" "
  "WHERE \"product_id\"=$1 AND \"farm_plot_id\"=$2";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::query_statement[] =
  "SELECT "
  "\"farm_plot_production_product\".\"quantity\", "
  "\"farm_plot_production_product\".\"production_now\", "
  "\"farm_plot_production_product\".\"product_id\", "
  "\"farm_plot_production_product\".\"farm_plot_id\" "
  "FROM \"farm_plot_production_product\"";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"farm_plot_production_product\"";

  const char access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::table_name[] =
  "\"farm_plot_production_product\"";

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;
    using pgsql::update_statement;

    callback (db, obj, callback_event::pre_update);

    pgsql::transaction& tr (pgsql::transaction::current ());
    pgsql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    // From FarmPlotProductionProduct.hpp:60:94
    const id_type& id (
      obj.id ());
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  erase (database& db, const id_type& id)
  {
    using namespace pgsql;

    ODB_POTENTIALLY_UNUSED (db);

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::pointer_type
  access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  find (database& db, const id_type& id)
  {
    using namespace pgsql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  reload (database& db, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    // From FarmPlotProductionProduct.hpp:60:94
    const id_type& id  (
      obj.id ());

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace pgsql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::object_type >
  access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) pgsql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::fasc::server::persistence::FarmPlotProductionProductEntity, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
