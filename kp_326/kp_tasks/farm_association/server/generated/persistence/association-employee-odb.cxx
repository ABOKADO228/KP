// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include "association-employee-odb.hxx"

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/pgsql/traits.hxx>
#include <odb/pgsql/database.hxx>
#include <odb/pgsql/transaction.hxx>
#include <odb/pgsql/connection.hxx>
#include <odb/pgsql/statement.hxx>
#include <odb/pgsql/statement-cache.hxx>
#include <odb/pgsql/simple-object-statements.hxx>
#include <odb/pgsql/container-statements.hxx>
#include <odb/pgsql/exceptions.hxx>
#include <odb/pgsql/simple-object-result.hxx>

namespace odb
{
  // AssociationEmployeeEntity
  //

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  persist_statement_name[] = "persist_fasc_server_persistence_AssociationEmployeeEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  find_statement_name[] = "find_fasc_server_persistence_AssociationEmployeeEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  update_statement_name[] = "update_fasc_server_persistence_AssociationEmployeeEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  erase_statement_name[] = "erase_fasc_server_persistence_AssociationEmployeeEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  query_statement_name[] = "query_fasc_server_persistence_AssociationEmployeeEntity";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  erase_query_statement_name[] = "erase_query_fasc_server_persistence_AssociationEmployeeEntity";

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  persist_statement_types[] =
  {
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::date_oid,
    pgsql::date_oid,
    pgsql::float8_oid,
    pgsql::text_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  find_statement_types[] =
  {
    pgsql::int8_oid
  };

  const unsigned int access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  update_statement_types[] =
  {
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::int8_oid,
    pgsql::date_oid,
    pgsql::date_oid,
    pgsql::float8_oid,
    pgsql::text_oid,
    pgsql::int8_oid
  };

  struct access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      pgsql::connection&,
      image_type&,
      id_image_type&,
      pgsql::binding&,
      pgsql::binding&,
      pgsql::native_binding&,
      const unsigned int*)
    {
    }
  };

  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::id_type
  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  id (const id_image_type& i)
  {
    pgsql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::id_type
  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  id (const image_type& i)
  {
    pgsql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        id,
        i.id_value,
        i.id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // id
    //
    t[0UL] = 0;

    // personId
    //
    t[1UL] = 0;

    // associationId
    //
    t[2UL] = 0;

    // roleId
    //
    t[3UL] = 0;

    // employmentStatusId
    //
    t[4UL] = 0;

    // hireDate
    //
    t[5UL] = 0;

    // dismissalDate
    //
    t[6UL] = 0;

    // salary
    //
    t[7UL] = 0;

    // contractNumber
    //
    if (t[8UL])
    {
      i.contractNumber_value.capacity (i.contractNumber_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  bind (pgsql::bind* b,
        image_type& i,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    std::size_t n (0);

    // id
    //
    if (sk != statement_insert && sk != statement_update)
    {
      b[n].type = pgsql::bind::bigint;
      b[n].buffer = &i.id_value;
      b[n].is_null = &i.id_null;
      n++;
    }

    // personId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.personId_value;
    b[n].is_null = &i.personId_null;
    n++;

    // associationId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.associationId_value;
    b[n].is_null = &i.associationId_null;
    n++;

    // roleId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.roleId_value;
    b[n].is_null = &i.roleId_null;
    n++;

    // employmentStatusId
    //
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.employmentStatusId_value;
    b[n].is_null = &i.employmentStatusId_null;
    n++;

    // hireDate
    //
    b[n].type = pgsql::bind::date;
    b[n].buffer = &i.hireDate_value;
    b[n].is_null = &i.hireDate_null;
    n++;

    // dismissalDate
    //
    b[n].type = pgsql::bind::date;
    b[n].buffer = &i.dismissalDate_value;
    b[n].is_null = &i.dismissalDate_null;
    n++;

    // salary
    //
    b[n].type = pgsql::bind::double_;
    b[n].buffer = &i.salary_value;
    b[n].is_null = &i.salary_null;
    n++;

    // contractNumber
    //
    b[n].type = pgsql::bind::text;
    b[n].buffer = i.contractNumber_value.data ();
    b[n].capacity = i.contractNumber_value.capacity ();
    b[n].size = &i.contractNumber_size;
    b[n].is_null = &i.contractNumber_null;
    n++;
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  bind (pgsql::bind* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].type = pgsql::bind::bigint;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  init (image_type& i,
        const object_type& o,
        pgsql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace pgsql;

    bool grew (false);

    // personId
    //
    {
      ::uint64_t const& v =
        o.personId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.personId_value, is_null, v);
      i.personId_null = is_null;
    }

    // associationId
    //
    {
      ::uint64_t const& v =
        o.associationId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.associationId_value, is_null, v);
      i.associationId_null = is_null;
    }

    // roleId
    //
    {
      ::uint64_t const& v =
        o.roleId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.roleId_value, is_null, v);
      i.roleId_null = is_null;
    }

    // employmentStatusId
    //
    {
      ::uint64_t const& v =
        o.employmentStatusId;

      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.employmentStatusId_value, is_null, v);
      i.employmentStatusId_null = is_null;
    }

    // hireDate
    //
    {
      ::fasc::server::domain::Date const& v =
        o.hireDate;

      bool is_null (false);
      pgsql::value_traits<
          ::fasc::server::domain::Date,
          pgsql::id_date >::set_image (
        i.hireDate_value, is_null, v);
      i.hireDate_null = is_null;
    }

    // dismissalDate
    //
    {
      ::odb::nullable< ::fasc::server::domain::Date > const& v =
        o.dismissalDate;

      bool is_null (true);
      pgsql::value_traits<
          ::odb::nullable< ::fasc::server::domain::Date >,
          pgsql::id_date >::set_image (
        i.dismissalDate_value, is_null, v);
      i.dismissalDate_null = is_null;
    }

    // salary
    //
    {
      ::odb::nullable< double > const& v =
        o.salary;

      bool is_null (true);
      pgsql::value_traits<
          ::odb::nullable< double >,
          pgsql::id_double >::set_image (
        i.salary_value, is_null, v);
      i.salary_null = is_null;
    }

    // contractNumber
    //
    {
      ::odb::nullable< ::std::basic_string< char > > const& v =
        o.contractNumber;

      bool is_null (true);
      std::size_t size (0);
      std::size_t cap (i.contractNumber_value.capacity ());
      pgsql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          pgsql::id_string >::set_image (
        i.contractNumber_value,
        size,
        is_null,
        v);
      i.contractNumber_null = is_null;
      i.contractNumber_size = size;
      grew = grew || (cap != i.contractNumber_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // id
    //
    {
      ::uint64_t& v =
        o.id;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.id_value,
        i.id_null);
    }

    // personId
    //
    {
      ::uint64_t& v =
        o.personId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.personId_value,
        i.personId_null);
    }

    // associationId
    //
    {
      ::uint64_t& v =
        o.associationId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.associationId_value,
        i.associationId_null);
    }

    // roleId
    //
    {
      ::uint64_t& v =
        o.roleId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.roleId_value,
        i.roleId_null);
    }

    // employmentStatusId
    //
    {
      ::uint64_t& v =
        o.employmentStatusId;

      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_value (
        v,
        i.employmentStatusId_value,
        i.employmentStatusId_null);
    }

    // hireDate
    //
    {
      ::fasc::server::domain::Date& v =
        o.hireDate;

      pgsql::value_traits<
          ::fasc::server::domain::Date,
          pgsql::id_date >::set_value (
        v,
        i.hireDate_value,
        i.hireDate_null);
    }

    // dismissalDate
    //
    {
      ::odb::nullable< ::fasc::server::domain::Date >& v =
        o.dismissalDate;

      pgsql::value_traits<
          ::odb::nullable< ::fasc::server::domain::Date >,
          pgsql::id_date >::set_value (
        v,
        i.dismissalDate_value,
        i.dismissalDate_null);
    }

    // salary
    //
    {
      ::odb::nullable< double >& v =
        o.salary;

      pgsql::value_traits<
          ::odb::nullable< double >,
          pgsql::id_double >::set_value (
        v,
        i.salary_value,
        i.salary_null);
    }

    // contractNumber
    //
    {
      ::odb::nullable< ::std::basic_string< char > >& v =
        o.contractNumber;

      pgsql::value_traits<
          ::odb::nullable< ::std::basic_string< char > >,
          pgsql::id_string >::set_value (
        v,
        i.contractNumber_value,
        i.contractNumber_size,
        i.contractNumber_null);
    }
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      pgsql::value_traits<
          ::uint64_t,
          pgsql::id_bigint >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::persist_statement[] =
  "INSERT INTO \"association_employee\" "
  "(\"id\", "
  "\"person_id\", "
  "\"association_id\", "
  "\"role_id\", "
  "\"employment_status_id\", "
  "\"hire_date\", "
  "\"dismissal_date\", "
  "\"salary\", "
  "\"contract_number\") "
  "VALUES "
  "(DEFAULT, $1, $2, $3, $4, $5, $6, $7, $8) "
  "RETURNING \"id\"";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::find_statement[] =
  "SELECT "
  "\"association_employee\".\"id\", "
  "\"association_employee\".\"person_id\", "
  "\"association_employee\".\"association_id\", "
  "\"association_employee\".\"role_id\", "
  "\"association_employee\".\"employment_status_id\", "
  "\"association_employee\".\"hire_date\", "
  "\"association_employee\".\"dismissal_date\", "
  "\"association_employee\".\"salary\", "
  "\"association_employee\".\"contract_number\" "
  "FROM \"association_employee\" "
  "WHERE \"association_employee\".\"id\"=$1";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::update_statement[] =
  "UPDATE \"association_employee\" "
  "SET "
  "\"person_id\"=$1, "
  "\"association_id\"=$2, "
  "\"role_id\"=$3, "
  "\"employment_status_id\"=$4, "
  "\"hire_date\"=$5, "
  "\"dismissal_date\"=$6, "
  "\"salary\"=$7, "
  "\"contract_number\"=$8 "
  "WHERE \"id\"=$9";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::erase_statement[] =
  "DELETE FROM \"association_employee\" "
  "WHERE \"id\"=$1";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::query_statement[] =
  "SELECT "
  "\"association_employee\".\"id\", "
  "\"association_employee\".\"person_id\", "
  "\"association_employee\".\"association_id\", "
  "\"association_employee\".\"role_id\", "
  "\"association_employee\".\"employment_status_id\", "
  "\"association_employee\".\"hire_date\", "
  "\"association_employee\".\"dismissal_date\", "
  "\"association_employee\".\"salary\", "
  "\"association_employee\".\"contract_number\" "
  "FROM \"association_employee\"";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::erase_query_statement[] =
  "DELETE FROM \"association_employee\"";

  const char access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::table_name[] =
  "\"association_employee\"";

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    {
      id_image_type& i (sts.id_image ());
      binding& b (sts.id_image_binding ());
      if (i.version != sts.id_image_version () || b.version == 0)
      {
        bind (b.bind, i);
        sts.id_image_version (i.version);
        b.version++;
      }
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj.id = id (sts.id_image ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace pgsql;
    using pgsql::update_statement;

    callback (db, obj, callback_event::pre_update);

    pgsql::transaction& tr (pgsql::transaction::current ());
    pgsql::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj.id);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  erase (database& db, const id_type& id)
  {
    using namespace pgsql;

    ODB_POTENTIALLY_UNUSED (db);

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::pointer_type
  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  find (database& db, const id_type& id)
  {
    using namespace pgsql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  reload (database& db, object_type& obj)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj.id);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace pgsql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::object_type >
  access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  query (database&, const query_base_type& q)
  {
    using namespace pgsql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += " ";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        sts.connection (),
        query_statement_name,
        text,
        false,
        true,
        q.parameter_types (),
        q.parameter_count (),
        q.parameters_binding (),
        imb));

    st->execute ();
    st->deallocate ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) pgsql::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::fasc::server::persistence::AssociationEmployeeEntity, id_pgsql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace pgsql;

    pgsql::connection& conn (
      pgsql::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement_name,
      text,
      q.parameter_types (),
      q.parameter_count (),
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
